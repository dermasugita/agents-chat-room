# AgentOrchestrator MVP 実装引き継ぎ文書

- **作業名**: `orchestrator-mvp`
- **日付**: 2026-07-26
- **読む順番**: この文書 → `CONTEXT.md`（用語集。本文書の用語はすべてここに従う）→ `docs/adr/`（決定の理由）
- **問い合わせ先**: 設計者へは `docs/session/orchestrator-mvp.jsonl` 経由。オーナーへは設計者が取り次ぐ

---

## 1. プロダクト概要

design-handoff ワークフロー（オーナー・設計者エージェント・実装者エージェントが、
CONTEXT / ADR / HANDOFF と非同期チャットを介して設計と実装を回す進め方）を、
**ファイル運用からサービスに移す**。

目的は**スケーラビリティ**である。1人のオーナーが同時に回せる設計–実装ラリーの本数を増やす。
バグ修正ではない。現行のファイル運用は動いているが、
「設計者と実装者が同じファイルシステムを共有する」前提と、
「規律で事故を防ぐ」構造が、本数を増やすと破綻する。

サーバは SQLite を持ち、**設計文書とスレッドの正本**を保持する。
設計者・実装者は CLI で、オーナーは web で接続する。
サーバは ssh ホスト上の docker で動き、クライアントは ssh local port forward 経由で到達する。

### ⚠️ 名前に反すること

ローカルの作業ディレクトリ名は `AgentOrchestrator` だが、
**サーバはエージェントをオーケストレーションしない。**
サーバは受動であり、エージェントプロセスの起動は一切行わない（[ADR 0002](../adr/0002-passive-server-per-thread-designers.md)）。
誰がいつエージェントを立ち上げるかはオーナーが決める。

### MVP スコープ外（作らないもの）

- **認証・ユーザ管理・マルチテナント**（[ADR 0004](../adr/0004-no-authentication.md)）。
  **ホストのループバックからしか到達できないこと**が唯一の防御（境界の置き場所は §2 の表）
- **サーバからのエージェント起動**（[ADR 0002](../adr/0002-passive-server-per-thread-designers.md)）
- **MCP サーバ**（[ADR 0003](../adr/0003-cli-only-client-and-polling.md)）。HTTP API の上に後から足せる
- **SSE / WebSocket / ロングポーリング**。新着検知は10秒ポーリングのみ
- **web からの文書編集**（[ADR 0007](../adr/0007-web-viewer-is-the-owner-endpoint.md)）。web で書けるのはスレッドへの発言だけ
- **サーバ側の差分適用・3-way マージ**（[ADR 0005](../adr/0005-full-text-put-with-optimistic-lock.md)）。文書更新は全文 PUT のみ
- **コードの管理**。コードの正本は GitHub にあり、サービスは一切関与しない
- **リアルタイム通知・メール・Slack 連携**
- **文書のリッチエディタ・プレビュー・全文検索**
- **Claude Code の transcript 取り込み**。インポート対象は既存のファイルベース文書のみ
- **CI 連携・PR 自動化**

---

## 2. アーキテクチャ

```
                    ssh ホスト（オーナー専用）
   ┌─────────────────────────────────────────────────┐
   │  docker container                               │
   │  ┌───────────────────────────────────────────┐  │
   │  │  ao-server (HTTP)                         │  │
   │  │    /api/v1/...   ← 設計者・実装者         │  │
   │  │    /            ← web ビューア（オーナー）│  │
   │  │  ┌─────────────────────────────────────┐  │  │
   │  │  │ SQLite: 文書の正本 / スレッド / 参加者│  │  │
   │  │  └─────────────────────────────────────┘  │  │
   │  └───────────────────────────────────────────┘  │
   │  内部 bind: 0.0.0.0:PORT（docker publish のため必須）│
   │  publish:  -p 127.0.0.1:PORT:PORT（host_ip 省略禁止）│
   └─────────────────────────────────────────────────┘
                          ▲
                          │ ssh local port forward
          ┌───────────────┴──────────────┐
          │                              │
   ローカルマシン                   ssh 先の開発環境
   ┌──────────────┐                ┌──────────────┐
   │ ao CLI       │                │ ao CLI       │
   │ 設計者エージェント│              │ 実装者エージェント│
   │ .ao/docs/ (写し)│              │ .ao/docs/ (写し)│
   └──────────────┘                └──────────────┘
          │                              │
          └──────── GitHub （コードの正本）────┘
```

### コンポーネント対応表

| コンポーネント | 役割 | 利用者 |
|---|---|---|
| `ao-server` | HTTP API + web ビューア + SQLite | — |
| `ao` CLI | 文書の pull/push、スレッドへの post、`watch` によるポーリング、注入、インポート | 設計者・実装者エージェント |
| web ビューア | 文書の現在状態とリビジョン履歴、会話履歴、横断の未回答 question 受信箱、発言 | オーナー（人間） |

### endpoint と参加者の対応

| 参加者 | endpoint | 心拍 | 離脱判定 |
|---|---|---|---|
| 設計者 | CLI | あり（`watch` のポーリング） | 対象 |
| 実装者 | CLI | あり（`watch` のポーリング） | 対象 |
| オーナー | web | **なし** | **対象外**（人間はポーリングしない） |

### 認証・認可

**無い。** 全エンドポイントが無認証で、参加者の識別子は自己申告である
（[ADR 0004](../adr/0004-no-authentication.md)）。

守るのは **「ホストのループバックからしか到達できないこと」の1点のみ**。
守る場所は配布形態ごとに違う。

| 配布形態 | プロセスの bind | 露出の境界 |
|---|---|---|
| **docker**（ssh ホストへの本番デプロイ） | コンテナ内 **`0.0.0.0`**（必須） | ホスト側 publish を **`127.0.0.1:PORT:PORT`** に限定 |
| **直接起動**（ローカル開発） | **`127.0.0.1`**（既定） | bind そのもの |

> **訂正（2026-07-26、実装者の実測による）**: 本節は当初
> 「サーバは 127.0.0.1 にのみバインドする。`0.0.0.0` を許す経路を実装に一切含めない」と
> 規定していた。**これは docker 配布と論理的に両立しない設計者の誤りである。**
> コンテナ内部で `127.0.0.1` に待ち受けると docker の publish が到達できず、
> サービスが疎通しない（実測: `curl` が exit 52 / `Empty reply from server`）。
> コンテナのネットワーク名前空間は隔離されているため、**内部の `0.0.0.0` は露出ではない。**

実装が守ること:

- bind アドレスは設定可能にしてよいが、**直接起動時の既定値は `127.0.0.1`**。
  引数も環境変数も無いときに `0.0.0.0` で待ち受けてはならない
- docker イメージの既定コマンドだけが `0.0.0.0` を指定する
- **`-p PORT:PORT`（host_ip 省略）を、文書・起動スクリプト・compose のどこにも書かない。**
  docker は host_ip を省略すると既定で `0.0.0.0` に公開する
- 起動時に `listening on <addr>:<port>` を必ずログへ出す。
  `0.0.0.0` にバインドする場合は、その行に警告を添える

---

## 3. API 仕様

すべて `/api/v1` 配下。リクエスト・レスポンスとも JSON。時刻は**サーバが UTC ISO8601 で記録する**
（クライアントの時刻は受け取らない。複数マシンの時計ズレでスレッドの順序が壊れるため）。

### プロジェクトと作業

| メソッド | パス | 備考 |
|---|---|---|
| `GET` | `/health` | サーバとスキーマのバージョンを返す |
| `GET` | `/projects` | 一覧 |
| `POST` | `/projects` | `{slug, name}`。slug 重複は 409 |
| `GET` | `/projects/:project` | 概要 + 作業一覧 + 文書一覧 |
| `POST` | `/projects/:project/works` | `{slug, title}` |
| `POST` | `/projects/:project/works/:work/resolve` | 作業を `resolved` にする。**スレッドは閉じない**（§5.5） |

### 文書

| メソッド | パス | 備考 |
|---|---|---|
| `GET` | `/projects/:project/documents` | 一覧（本文なし。kind / slug / title / current_revision） |
| `GET` | `/projects/:project/documents/:doc` | `{body, revision, title, updated_at, author}` |
| `GET` | `/projects/:project/documents/:doc/revisions` | リビジョン一覧（本文なし） |
| `GET` | `/projects/:project/documents/:doc/revisions/:n` | 指定リビジョンの全文 |
| `POST` | `/projects/:project/documents` | 新規作成。`{kind, slug?, title, body, author}` |
| `PUT` | `/projects/:project/documents/:doc` | 更新。`{body, base_revision, author, note?}` |

`:doc` は `context` / `adr/0003-...` / `handoff/orchestrator-mvp` の形。

**`PUT` の応答規約**:

- `base_revision` がサーバの現在リビジョンと**厳密に一致**すれば 200。新しいリビジョン番号を返す
- 一致しなければ **409**。ボディに `{current_revision, current_body}` を含める
  （クライアントが即座に編集を当て直せるようにするため。往復を1回減らす）
- 差分適用も自動マージも**行わない**（[ADR 0005](../adr/0005-full-text-put-with-optimistic-lock.md)）

**ADR 作成時の採番**: `kind=adr` で `POST` した場合、`slug` は指定させず**サーバが採番**する。
プロジェクト内で単調増加する番号を採り、`NNNN-<title を kebab-case 化>` を slug とする。
設計者が複数いるため、クライアント側で番号を決めさせてはならない。

**CONTEXT の一意性**: `kind=context` の文書はプロジェクトに高々1つ。2つ目の作成は 409。

### スレッド

| メソッド | パス | 備考 |
|---|---|---|
| `GET` | `/projects/:project/works/:work/messages?since=<seq>` | `since` より後のメッセージ |
| `POST` | `/projects/:project/works/:work/messages` | 投稿。下記参照 |
| `POST` | `/projects/:project/works/:work/messages/:seq/close` | question を明示クローズ。**投稿者本人のみ** |
| `GET` | `/projects/:project/works/:work/poll?since=<seq>&as=<identifier>[&role=<role>]` | **心拍を兼ねる**。§3.1 |
| `GET` | `/inbox?as=<identifier>` | 全プロジェクト横断の、自分宛の未回答 question |

> **訂正（2026-07-26、実装者の指摘による）**: これらのパスは当初 `/works/:work/...` と
> プロジェクトを含まない形で書いていた。**これは設計者の誤りである。**
> `work` の一意制約は `(project_id, slug)` であり、別プロジェクトに同名の作業を作れる。
> プロジェクトを含まないパスでは同名の作業を識別できず、参照先が不定になる。
>
> 却下した代案:
> - **`work.slug` をサービス全体で一意にする** — スキーマは単純になるが、
>   「プロジェクトの下に作業がぶら下がる」という `CONTEXT.md` の定義と矛盾する。
>   別プロジェクトで同じ作業名（`orchestrator-mvp` など）を使えないのは不自然
> - **`:work` を内部 ID にする** — 一意にはなるが、人もエージェントも打てず、
>   ログやチャットの `refs` に書いても読めない

**参加者識別子のスコープ**: `participant` 行は作業ごとに作られる（`UNIQUE (work_id, identifier)`）。
これは心拍と初出をスレッド単位で追うためであり、**識別子の文字列そのものはサービス全体で
同一人物を指す規約とする**。`/inbox?as=designer-a` は、全プロジェクト・全作業を横断して
`designer-a` 宛の未回答 question を集める。同じ文字列を別人に使い回してはならない。

**`POST /projects/:project/works/:work/messages` のリクエスト**:

```json
{
  "idempotency_key": "<クライアント生成の UUID>",
  "from": "designer-a",
  "role": "designer",
  "type": "message|question|answer|decision|status|resolve",
  "body": "本文。改行を含んでよい",
  "to": ["impl-a", "impl-b"],
  "reply_to": 12,
  "refs": ["src/server/db.ts", "https://github.com/.../pull/3"],
  "expects": [{"doc": "context", "revision": 7}],
  "ball": ["impl-a"]
}
```

- **`seq` はサーバが採番する。** 作業内で 1 から連番。クライアントは ID を計算しない
  （現行運用で最大の事故源だった「末尾行 +1」の衝突を原理的に消す）
- **`idempotency_key`**: 同一作業内で同じキーの投稿は、新規作成せず**既存メッセージをそのまま返す**。
  応答が失われて CLI が再送したときの二重投稿を防ぐ
- **`type=question` は `to` が空であってはならない**（400）。誰のボールにもならない質問を作らないため
- **`type=answer` は `reply_to` 必須**（400）
- `expects` は期待バージョン。受け手が自分の認識とのズレに気づくためのもので、**書き込みを拒否しない**
- `ball` は宣言ボール（§5.3）
- **参加者は事前登録しない。** 未知の `from` は、その `role` で参加者として自動登録される

### 3.1 `poll` — 新着取得・心拍・状態通知を兼ねる

新着取得のエンドポイント。

**心拍を打つのは、エージェントが能動的に叩いた呼び出しだけである**
（[ADR 0006](../adr/0006-ball-tracking-and-heartbeat.md) の訂正）。
リクエストに `heartbeat=false` を付けられるようにし、
**常駐の `ao watch` ループはこれを付けて心拍を更新しない。**

> **訂正の背景（ドッグフーディング中に実証）**: 当初「このリクエストが心拍を兼ねる」と
> 規定していたが、常駐 `ao watch` はエージェントのターンが終わった後もポーリングを続け、
> **誰も読んでいないのに心拍を打ち続ける。** Codex 系ランタイムには Monitor 相当の
> 通知機構が無く、エージェントは自分で確認しない限り新着に気づかない。
> 実際に `ui-implementer` の心拍が1秒前を示す一方、作業ツリーは9分間無変化だった。
> 心拍が測っていたのはプロセスの生存であって、エージェントの注意の生存ではなかった。

| 呼び出し元 | 心拍 |
|---|---|
| `ao post` / `pull` / `push` / `messages` / `close` / `resolve` | **打つ** |
| `ao watch --once`（エージェントが自分の意思で1回叩く） | **打つ** |
| 常駐 `ao watch` のループ | **打たない**（`heartbeat=false`） |

**`role` の扱い**（当初 §3 に記載が無く、実装との食い違いを実測で発見したため追記）:

- `as` の参加者が**まだ存在しない**場合、`role` は**必須**（400）。参加者はここで自動登録される
- `as` の参加者が**既に存在する**場合、`role` は**省略可**。省略時は登録済みのロールを使う
- `role` を渡し、それが登録済みのロールと**異なる**場合は 409。
  エラー本文には**要求されたロールと登録済みのロールの両方**を含めること
  （「designer is already registered as designer」のように、
  何を要求したのか分からない文言にしない）

省略可にする理由は、既存参加者にとって `role` は冗長であり、
毎回のポーリングで送らせると「送り忘れ・取り違え」が起きるため。
CLI は `.ao/config.json` から常に送ってよいが、API 側で必須にはしない。

応答:

```json
{
  "messages": [ ... since より後の新着 ... ],
  "your_ball": {
    "has_ball": true,
    "reasons": [
      {"kind": "unanswered_question", "seq": 12, "from": "designer-a"},
      {"kind": "declared", "seq": 15, "by": "designer-a"}
    ]
  },
  "idle_nudge": null,
  "abandoned": [
    {"identifier": "impl-b", "last_heartbeat_at": "...", "ball_reasons": [...]}
  ],
  "stale_expectations": [
    {"doc": "context", "you_have": 7, "current": 9}
  ]
}
```

- `your_ball` — 導出ボールと宣言ボールを合わせた、**この参加者の**ボール状態
- `idle_nudge` — 手空きが5分続いたときに文言を入れる。それ以外は `null`（§5.4）
- `abandoned` — **他の**参加者のうち離脱状態のもの（§5.4）。設計者がオーナーへ上げる材料
- `stale_expectations` — この参加者が直近のメッセージで宣言した期待バージョンが、
  現在のリビジョンより古い場合に入れる。pull を促す

### 削除（2026-07-26 追加・オーナー指示）

| メソッド | パス | 備考 |
|---|---|---|
| `DELETE` | `/projects/:project?confirm=<slug>` | プロジェクトと配下すべてを削除 |
| `DELETE` | `/projects/:project/works/:work?confirm=<slug>` | 作業とそのスレッドを削除 |
| `DELETE` | `/projects/:project/works/:work/participants/:identifier` | 参加者行の削除 |

**なぜ必要か（実際に起きたこと）**:

- 検証用の使い捨て参加者 `watch-probe` を消せず、設計者が**生 SQL を書いて削除した**
- サーバ解決順の誤り（基準32 の訂正）により、実装者のテストが本番へ書き込み、
  **`Legacy CLI` という名前のプロジェクトが4件と、空のプロジェクトが1件、本番に生えた**

運用していれば必ずゴミは出る。**消す手段が無い設計は、運用に耐えない。**

**規約**:

- **`confirm` クエリに対象の slug を渡さない限り実行しない**（400）。取り違えを防ぐ
- **中身のあるものは、追加の明示なしに消せない**（下記「実際に起きた事故」）
- 削除は**カスケードする**（プロジェクト → 作業 → メッセージ / 参加者 / 文書 / リビジョン）。
  外部キーの順序を守り、**トランザクションで行う**。中途半端な状態を残さない
- **応答に、何を何件消したかの内訳を返す**（`{works: n, messages: n, documents: n, ...}`）。
  「消えた」だけでは確認できない
- 参加者の削除は、**その識別子の発言が1件でもあれば拒否する**（409）。
  発言者が消えるとスレッドが読めなくなる。誤登録された未発言の参加者だけが対象

#### 実際に起きた事故: `confirm` は「消してよいか」を守らない（2026-07-26）

**設計者が、オーナーが稼働中に使っていたルームを削除した。**

経緯:

1. 設計者は 08:41 の一覧で `medopsanonymizer` を「文書0・作業0」と見て、
   検証で生じたゴミだと分類した
2. 削除の直前には**文書5・作業2 に増えていた。**
   設計者はその数値を自分で画面に出力しながら、「（was 0/0）」と書いただけで
   **最初の分類を見直さなかった。増えていること自体が「使われている」証拠だった**
3. **中身を1件も確認せずに**、7件を一括ループで削除した
4. 削除された内容は、作業2件・メッセージ13件・文書5件（ADR 2本と HANDOFF 2本を含む）

**`confirm` は機能していた。** 対象 slug の完全一致を要求し、不一致は 400 で弾いていた。
しかし `confirm` が防ぐのは**タイプミス**だけであり、
**「これは消してよいものか」という判断は防げない。** そこを設計者が誤った。

したがって、次を実装で強制する。

- **削除対象にメッセージが1件でもあれば、既定で拒否する（409）。**
  応答に「何が失われるか」を含める: 作業ごとのメッセージ数、最終更新時刻、
  心拍のある参加者の識別子
- 本当に消す場合は、**`confirm` とは別の明示的な指定**を要求する
  （`--delete-nonempty` 相当）。`confirm` の使い回しで通ってはならない
- **CLI は削除前に、失われる内容の要約を必ず表示する。**
  空の対象であっても表示する（「空である」という情報自体が判断材料になる）

**空かどうかを呼び出し側の記憶に頼らせない。** 設計者は「空だったはず」という
20分前の記憶で消した。サーバは現在の状態を知っているので、サーバが答えるべきである。

**メッセージ単体の削除は作らない。** `CONTEXT.md` の「追記のみ、編集・削除はできない」を
崩さない。作業ごと消すのは容器に対する管理操作であって、進行中の会話の改竄ではない。
この区別を実装でも保つこと。

**web からは削除できない**（[ADR 0007](../adr/0007-web-viewer-is-the-owner-endpoint.md)）。
web で書けるのはスレッドへの発言だけ、という位置づけを変えない。削除は CLI から行う。

### インポート

| メソッド | パス | 備考 |
|---|---|---|
| `POST` | `/import` | ファイルベース文書の取り込み。§6 |

---

## 4. データモデル

SQLite。DB ファイルは1つ（プロジェクトごとに分けない）。

```sql
PRAGMA journal_mode  = WAL;      -- web と CLI が同時に読むため
PRAGMA foreign_keys  = ON;
PRAGMA busy_timeout  = 5000;     -- ★ 必須。理由は下記

CREATE TABLE project (
  id          INTEGER PRIMARY KEY,
  slug        TEXT NOT NULL UNIQUE,
  name        TEXT NOT NULL,
  created_at  TEXT NOT NULL                    -- UTC ISO8601
);

CREATE TABLE work (
  id          INTEGER PRIMARY KEY,
  project_id  INTEGER NOT NULL REFERENCES project(id),
  slug        TEXT NOT NULL,
  title       TEXT NOT NULL,
  state       TEXT NOT NULL CHECK (state IN ('open','resolved')),
  created_at  TEXT NOT NULL,
  UNIQUE (project_id, slug)
);
-- スレッドは作業に1つなので独立したテーブルを持たない。message.work_id がスレッドを表す。

CREATE TABLE document (
  id               INTEGER PRIMARY KEY,
  project_id       INTEGER NOT NULL REFERENCES project(id),
  kind             TEXT NOT NULL CHECK (kind IN ('context','adr','handoff')),
  slug             TEXT NOT NULL,
  work_id          INTEGER REFERENCES work(id),   -- handoff のみ非 NULL
  adr_number       INTEGER,                       -- adr のみ非 NULL
  title            TEXT NOT NULL,
  current_revision INTEGER NOT NULL,
  created_at       TEXT NOT NULL,
  UNIQUE (project_id, kind, slug)
);
CREATE UNIQUE INDEX idx_single_context ON document(project_id) WHERE kind = 'context';
CREATE UNIQUE INDEX idx_adr_number     ON document(project_id, adr_number) WHERE kind = 'adr';

-- リビジョンは全文スナップショット（差分ではない）。文書は小さく、任意リビジョンの閲覧が単純になる。
CREATE TABLE revision (
  id          INTEGER PRIMARY KEY,
  document_id INTEGER NOT NULL REFERENCES document(id),
  revision    INTEGER NOT NULL,                  -- 文書ごとに 1 から単調増加
  body        TEXT NOT NULL,
  author      TEXT NOT NULL,
  note        TEXT,
  created_at  TEXT NOT NULL,
  UNIQUE (document_id, revision)
);

-- 行は作業ごとだが、identifier の文字列はサービス全体で同一人物を指す規約。
-- /inbox はこの文字列で全プロジェクトを横断して引く（§3 の「参加者識別子のスコープ」）。
CREATE TABLE participant (
  id                INTEGER PRIMARY KEY,
  work_id           INTEGER NOT NULL REFERENCES work(id),
  identifier        TEXT NOT NULL,
  role              TEXT NOT NULL CHECK (role IN ('owner','designer','implementer')),
  first_seen_at     TEXT NOT NULL,
  last_heartbeat_at TEXT,                        -- owner は常に NULL
  UNIQUE (work_id, identifier)
);

CREATE TABLE message (
  id              INTEGER PRIMARY KEY,
  work_id         INTEGER NOT NULL REFERENCES work(id),
  seq             INTEGER NOT NULL,              -- 作業内で 1 から連番。サーバ採番
  idempotency_key TEXT NOT NULL,
  from_identifier TEXT NOT NULL,
  type            TEXT NOT NULL
                  CHECK (type IN ('message','question','answer','decision','status','resolve')),
  body            TEXT NOT NULL,
  reply_to_seq    INTEGER,
  closed_at       TEXT,                          -- question の明示クローズ
  created_at      TEXT NOT NULL,
  UNIQUE (work_id, seq),
  UNIQUE (work_id, idempotency_key)
);

CREATE TABLE message_to (
  message_id INTEGER NOT NULL REFERENCES message(id),
  identifier TEXT NOT NULL,
  PRIMARY KEY (message_id, identifier)
);

CREATE TABLE message_ref (
  message_id INTEGER NOT NULL REFERENCES message(id),
  ref        TEXT NOT NULL
);

-- 期待バージョン。書き込みを拒否しないので制約は張らない。
CREATE TABLE message_expects (
  message_id  INTEGER NOT NULL REFERENCES message(id),
  document_id INTEGER NOT NULL REFERENCES document(id),
  revision    INTEGER NOT NULL,
  PRIMARY KEY (message_id, document_id)
);

-- 宣言ボール。「最後に宣言を伴ったメッセージ」の集合が現在の宣言ボール集合になる。
CREATE TABLE ball_declaration (
  message_id INTEGER NOT NULL REFERENCES message(id),
  identifier TEXT NOT NULL,
  PRIMARY KEY (message_id, identifier)
);
```

### `busy_timeout` は必須（実測。§7.4 クローズ済み）

**WAL だけでは足りない。すべての DB 接続に `busy_timeout=5000` を設定すること。**
接続を作る箇所が複数あるなら、接続生成を1関数に閉じて全経路が通るようにする。

実装者の実測（Node v22.14.0 / `node:sqlite`、watch 相当8本 + web 読み取り1本 + 書き込み1本を並行）:

| 設定 | 結果 |
|---|---|
| `busy_timeout=0` | 8,490 試行中 **3,338 件が `database is locked`**（成功 5,152） |
| `busy_timeout=5000` | **11,400 操作すべて成功**（busy 0 / その他エラー 0） |

WAL は読み書きの同時実行を許すが `SQLITE_BUSY` を消しはしない。
10秒ポーリングが N 本走る本サービスでは、設定しなければ確実に落ちる。

### 保存先の分担

| データ | 正本 | 備考 |
|---|---|---|
| CONTEXT / ADR / HANDOFF | **この DB** | [ADR 0001](../adr/0001-documents-source-of-truth-in-db.md) |
| スレッドのメッセージ | **この DB** | |
| **コード** | **GitHub** | サービスは関与しない |
| 写し（`.ao/docs/`） | — | 正本ではない。読む場・編集の下書き場 |

---

## 5. コアロジック仕様

### 5.1 文書の pull / push

```
pull:
  1. GET /documents/:doc → {body, revision}
  2. 写しの先頭にヘッダを付けて .ao/docs/ 配下へ書き出す
  3. .ao/state.json に {doc: revision} を記録する

push:
  1. .ao/docs/ の写しを読み、ヘッダを取り除く
  2. .ao/state.json の revision を base_revision として PUT
  3. 200 → state.json の revision を更新
  4. 409 → 応答の current_body で写しを更新し、非ゼロ終了。
          「pull し直したので、同じ編集を当て直して再度 push せよ」と出力する
```

**写しのヘッダ**（各ファイル先頭）:

```
<!-- AgentOrchestrator: これは写しです。正本はサーバ側 DB にあります。
     revision=7 / doc=context / pulled=2026-07-26T04:00:00Z
     編集しても `ao push` するまで正本に反映されません。`ao pull` で失われます。 -->
```

`pull` はローカルの未 push 編集を破棄しうる。**写しの内容が state.json 記録時から変わっていたら、
`--force` が無い限り中断する。**

### 5.2 ボールの導出

参加者 P が作業 W でボールを持つのは、次のいずれかが成り立つとき。

**導出ボール（サーバが判定。宣言では消せない）**

> `type=question`、`closed_at IS NULL` のメッセージ M があり、
> P が M の宛先に含まれ、かつ P が「`type=answer` かつ `reply_to_seq = M.seq`」のメッセージを
> まだ書いていない。

**宛先ごとに独立して判定すること。** 宛先が `["impl-a","impl-b"]` の question に `impl-a` が
答えても、`impl-b` のボールは残る。「誰かが答えた」を全員の免責にしてはならない。
残りを免責したい場合は、質問者が明示的に `close` する。

**宣言ボール**

> 作業 W の中で、`ball_declaration` を伴う**最新の**メッセージの宣言集合に P が含まれる。

宣言は上書き式である（過去の宣言は現在の状態に影響しない）。
現在のボール集合 = 導出ボール ∪ 宣言ボール。

### 5.3 心拍と離脱

- `GET /projects/:project/works/:work/poll?as=P` のうち、**`heartbeat=false` が付いていないもの**を
  受けたときに、P の `last_heartbeat_at` を現在時刻で更新する。
  常駐 `ao watch` のループは `heartbeat=false` を付けるので、心拍を偽装しない（§3.1 の訂正）
- **離脱**: P がボールを持ち、かつ `last_heartbeat_at` が **3分**より古い（ポーリング間隔10秒に対し
  18回分の欠落。瞬断では誤検知しない）。`role='owner'` は判定対象外（人間はポーリングしない）
- 離脱は他の参加者の `poll` 応答の `abandoned` に載る。サーバから外へ通知はしない（受動サーバ）

### 5.4 手空き通知

- **手空き**: P がボールを持たず、かつ作業 W の最新メッセージが **5分**より古い
- 手空きが検出されたら、P の `poll` 応答の `idle_nudge` に文言を入れる
- **スレッドには書き込まない。** その参加者にだけ届く
- P が何か書けば「最新メッセージが5分より古い」が偽になるので、通知は連続で暴発しない
- 設計者にも実装者にも同じ条件で出す（どちらの側でも同じ思い込みが起きる）

### 5.5 resolve

- `POST /projects/:project/works/:work/resolve` で `state='resolved'` にする
- **スレッドは閉じない。メッセージの投稿は resolve 後も受け付ける**
- **resolve 済みの作業でも、離脱判定と手空き通知は動き続ける。**
  resolve を根拠に離脱したエージェントは、そのまま `abandoned` に載る
- 状態は `open` / `resolved` の2つだけ。`closed` を持たない
  （「resolve は終了ではない」という規定と衝突するため）

### 5.6 期待バージョンの照合

メッセージの `expects` は保存するだけで、投稿時には何も検査しない。
検査は `poll` 応答の `stale_expectations` で行う: 参加者が直近に宣言した期待バージョンが
現在のリビジョンより古ければ、そこに載せて pull を促す。

**期待バージョンは拒否を起こさない。拒否を起こすのは文書 PUT の `base_revision` だけ。**
この2つを混同しないこと（`CONTEXT.md` の「期待バージョン」「基底リビジョン」の項を参照）。

---

## 6. 注入とインポート

### 6.1 `ao inject <path/to/target/repo>`

対象リポジトリをサービス管理下に置く（[ADR 0008](../adr/0008-injection-into-target-repositories.md)）。

1. サーバにプロジェクトを登録（既にあれば再利用）。
   **`--work` が指定されたら作業も同時に登録する**（既にあれば再利用）。

   > **追記（2026-07-26、実測で発見）**: 当初これを書いていなかったため、実装は
   > `--work` を受け取って `.ao/config.json` に書きながら、サーバ側に作業を作らなかった。
   > 結果、注入直後のリポジトリが存在しない作業を指し、`ao post` や `ao watch` が
   > `Work not found` で落ちる。**失敗が inject の時点ではなく最初の操作まで遅れて現れる**
   > のが悪い。ADR 0008 が却下した「手順書にすると必ず抜ける」が、この形で顕在化した。
2. `<repo>/.ao/config.json` を書く — サーバ URL、プロジェクト slug、参加者識別子、ロール
3. **サービス版の手順を4箇所すべてへ配置**（[ADR 0008](../adr/0008-injection-into-target-repositories.md)）。
   参加者のランタイムは Claude Code とは限らないため（本 MVP の実装者は Codex Desktop 上にいる）:

   | 配置先 | 形 | 注意 |
   |---|---|---|
   | `<repo>/.claude/skills/<name>/` | 手順一式 | 既存を黙って上書きしない |
   | `<repo>/.agents/skills/<name>/` | 同じ内容 | 同上 |
   | `<repo>/CLAUDE.md` | **追記のみ** | 全置換しない。マーカーで挟み、再注入で重複させない |
   | `<repo>/AGENTS.md` | **追記のみ** | 同上 |

   **同じ内容を複数箇所へ配るので、必ず単一のソースから生成すること。**
   手で複製すると片方だけ更新されて必ず食い違う
4. `<repo>/.gitignore` に `/.ao/` を追記（既にあれば何もしない）
5. 文書の写しを `<repo>/.ao/docs/` へ pull
6. 対象リポジトリに既存のファイルベース文書があればインポート候補として提示する（実行はしない）

**注入は追記のみで、既存の内容を破壊しない。** これはオーナーが前提としている保証であり、
実装が満たしていることを実測で確認済み（既存 `CLAUDE.md` の内容とオーナー自作の
`.claude/skills/my-own-skill/` が2回注入後も無傷）。

- `CLAUDE.md` / `AGENTS.md` — マーカーで挟んだブロックを**追記**。全置換しない
- `.gitignore` — `/.ao/` の行を**追記**（既にあれば何もしない）
- スキルファイル — 既存を黙って上書きしない。差分を示して確認を取るか `.bak` へ退避する。
  オーナーが手で育てたスキルを消してはならない

この保証があるため、**移植元のリポジトリに注入しても安全**である
（アーカイブ済みのリポジトリから履歴を移す場合も、既存の内容は失われない）。

### 6.2 サービス版の手順に書き換える内容

配置先は §6.1 の4箇所。**内容は単一のソースから生成する。**

現行スキル（`design-handoff` / `session-chat` / `grill-with-docs`）から、
**廃止される前提を残らず除去**し、`ao` の手順に差し替える。除去対象:

| 現行の記述 | 差し替え後 |
|---|---|
| `jq -nc` で1行 JSON を組み立てる | `ao post --type ... --body ...` |
| `msg-%04d` を末尾行 +1 で採番 | サーバ採番。クライアントは ID を計算しない |
| ID 衝突時は `msg-0024/designer` と書いて区別（SESSION-PROTOCOL §2.5） | **節ごと削除**（衝突は起きない） |
| `wc -l` の行数ポーリング / `tail -F` 禁止 | `ao watch`（10秒ポーリング） |
| `docs/session/<作業名>.jsonl` を読む | `ao messages` / `ao watch` |
| CONTEXT / ADR / handoff をファイルとして編集 | `ao pull` → 写しを編集 → `ao push`。409 の手順を明記 |
| 「ポーリングのたびにボール所在を自問せよ」 | `poll` 応答の `your_ball` を読む（自問は補助に降格） |
| 「実装者の沈黙を待ちと解釈しない」ための差分監視手順 | `abandoned` と `idle_nudge` を読む |

### 6.2.1 スキルは「通知が来ない前提」で書くこと（最重要・実証済み）

**「起こされる」には2種類あり、混同してはならない。**

| 種類 | 何が起こすか | 例 |
|---|---|---|
| **イベント駆動** | バックグラウンドプロセスの出力が、その瞬間にエージェントを起こす | Claude Code の Monitor |
| **時刻駆動** | あらかじめ登録した間隔で、エージェントが自分を起こす | Codex の**カスタム スケジュール**（英語 UI では Scheduled tasks） |

**Codex にはイベント駆動の機構が無い。** したがって `ao watch` をバックグラウンドで
走らせても、その出力がエージェントを起こすことはない。
スキルが「`ao watch` を起動して待て」とだけ書くと、Codex のエージェントは
**プロセスだけ動かしてターンを終え、以後まったく応答しなくなる。**

> **訂正（実装者の指摘・2026-07-26）**: 本節は当初「Codex 系には相当する機構が無い」と
> 書いていたが、これは不正確だった。無いのは**イベント駆動**の機構であって、
> **時刻駆動の自己起動（カスタム スケジュール）は存在する。**
> 同じ節の後半の表と矛盾していたため、上記の2分類に書き直した。

これは仮定ではない。ドッグフーディング中に実際に起きた（§3.1 の訂正注記）。

サービス版スキルの監視の節は、次を満たすこと。

1. **自己駆動ループを手順として書く。**
   「作業を一定量進める → `ao watch --once` を叩く → 新着があれば処理する → 繰り返す」
   という形を、コピペできる具体例つきで示す
2. **長い作業の最中でも定期的に `ao watch --once` を叩かせる。**
   目安（数分ごと、あるいはファイル1つの編集が終わるごと）を明示する
3. **常駐 `ao watch` は「Claude Code での補助」と位置づけ、それだけに頼らせない。**
   常駐版は心拍を打たないことも書く
4. **心拍の意味を本人に伝える。**
   「あなたが `ao watch --once` を叩かない限り心拍は途絶える。
   ボールを持ったまま心拍が途絶えると離脱として扱われ、オーナーに通知される」と
   明示する。自分の行動が観測されていることを知らせる
5. **ボール判定を、叩くたびに行わせる。**
   `poll` 応答の `your_ball` を読み、自分にボールがあれば待たずに作業を続ける

**「待て」で終わる指示を書かないこと。** 待ち方を具体的に書く。

#### ランタイム別の起こし方を、そのランタイムの言葉で書く

「自分で定期的に確認せよ」と抽象的に書いても、エージェントは**自分のランタイムで
それをどう実現するか**を知らない。**各ランタイムが提供する機能の正式名称で書くこと。**

| ランタイム | 起こす手段 | スキルに書く言葉 |
|---|---|---|
| **Codex** | **時刻駆動**。アプリの**「カスタム スケジュール」**機能（日本語 UI 名はオーナー提供の情報。英語 UI では **Scheduled tasks**、実装者が一次情報で確認済み） | 両方の名称を書く。「カスタム スケジュール（Scheduled tasks）で一定間隔の自己確認を登録する」 |
| **Claude Code** | Monitor ツール（`ao watch` の出力で起こされる）+ 自己駆動ループ | Monitor は補助であり、それだけに頼らない旨も書く |
| その他 / 不明 | 自己駆動ループのみ | 作業の区切りごとに `ao watch --once` を叩く |

**機能の正式名称は推測しない。** 分からないランタイムについては「自己駆動ループを回す」に留め、
オーナーに確認して判明した時点で追記する。誤った機能名を書くと、
エージェントは存在しない機能を探して時間を溶かす。

### 6.2.2 試行錯誤で分かったことは、散文でなく実行可能なスクリプトとして同梱する

オーナーの指示（2026-07-26）:
「CLI の試行錯誤をしていると思いますが、うまくいったものは skills の built-in script として
固めてくださいね」。

**スキルに「こう書け」と散文で書くだけでは、同じ事故が繰り返される。**
実際にドッグフーディング中、設計者が次の4つを踏んだ。すべて「知っていれば避けられた」類であり、
**知識ではなくスクリプトとして配るべきもの**である。

| 実際に起きた事故 | 原因 | スクリプト化で防ぐ形 |
|---|---|---|
| 投稿が失敗したのに気づかず、指示が相手に届いていなかった | `ao post ... \| grep '"seq"'` としたため、`Server returned 400: answer messages require reply_to` が stderr へ出て見えず、パイプの終了コードも `grep` のものになっていた | **投稿ラッパ**を同梱し、終了コードを必ず判定して失敗を標準エラーへ出す。パイプで握り潰せない形にする |
| スレッド監視が壊れ、オーナーの発言2件を取りこぼした | 監視ループが `jq -e '.messages'` で成否を判定していたが、日本語本文を含む応答で誤判定した | **監視スクリプト**を同梱する。`jq` に依存しない実装にし、取得失敗と「新着ゼロ」を区別する |
| DB のバックアップが空だった | WAL モードのため実データが `-wal` 側にあり、`ao.sqlite` を `cp` しても中身が入っていなかった（本体 4KB / WAL 1.6MB） | **バックアップスクリプト**を同梱し、**`VACUUM INTO`** を使う（下記5）。取得後に件数を検証して表示する |
| エージェントが応答不能なのに心拍が出ていた | 常駐 `ao watch` に頼り、自己駆動ループを回していなかった | §6.2.1 の**自己駆動ループをコピペ可能なスクリプト**として同梱する |

同梱するもの（配置はスキルの一部として `inject` が配る）:

1. **投稿ラッパ** — 型ごとの必須項目（`answer` は `reply_to`、`question` は `to`）を
   送信前に検査し、サーバの 400 を待たずに落とす。終了コードを呼び出し元へ正しく返す
2. **監視スクリプト** — `jq` 非依存。取得失敗を「新着ゼロ」と混同しない。
   連続失敗を出力に出す。**心拍を打たないモード**（§3.1）で動く
3. **自己駆動ループのテンプレ** — 「作業 → `ao watch --once` → 新着処理 → 繰り返す」の
   実行可能な雛形。エージェントがコピーして自分の作業ループに埋められる形
4. **ボール確認のワンライナー** — `your_ball` と手空き通知を読み、
   自分にボールがあるかを1行で判定する

**設計者・オーナー向けの運用スクリプトは、スキルではなくリポジトリの `scripts/` へ置く**
（エージェントに配る必要が無いため）:

5. **DB バックアップ** — **`VACUUM INTO` を使う**。取得後に各テーブルの件数を表示して
   中身が空でないことを確認する。`cp` を使う手順を README に書かない

   > **`node:sqlite` の `backup` API を使わない理由（実測）**: `backup` は Node 22.16 以降の
   > エクスポートであり、CLI の下限である **22.14 には存在しない**
   > （v22.14.0 の exports は `DatabaseSync, StatementSync, constants` のみ）。
   > 一方 `VACUUM INTO` は SQL 文なので Node の API バージョンに依存しない。
   > v22.14.0 で WAL モードの DB（日本語を含む500件）に対して実行し、
   > バックアップ側で件数と末尾レコードが一致することを確認済み。
   > **バックアップだけ Node バージョン要件を分ける必要は無い。**
   >
   > 注意: `VACUUM INTO` はトランザクションの内側では実行できない。
   >
   > **件数の表示を省かないこと。** `cp` で取ったバックアップは 4KB のファイルとして存在し、
   > 一見成功に見えた。中身が空だと分かったのは後から件数を数えたからである。
   > **ファイルが存在することはバックアップが成功したことを意味しない。**
6. **デプロイ** — §8 の `docker run` をスクリプト化する。
   `-p PORT:PORT`（host_ip 省略）を書けない形にする

**残すもの**（サービス化で不要にならない規律）:

- 文書の役割分担（CONTEXT は用語集、ADR はなぜ、HANDOFF は何を・どう）
- 文書先行（決定はチャットで終わらせず、先に文書を更新してから返す）
- 判断の三分類（設計者が決める / オーナーに聞く / 実装者に委ねる）
- マイルストーン報告の独立検証、「テスト通過を完了の証拠にしない」「成果物をディスク上に実見する」
- **resolve は終了ではない。離脱の許可を出せるのはオーナーだけ**
- シークレットを書かない

### 6.2.3 実装者は「スキルを呼ぶだけ」で参加できること（オーナー指示・2026-07-26）

オーナーの言葉:
「implementer 側の skill は突然 skill を呼ぶだけで良いようにしたい。
1往復だけ、どの chat room に入れば良いかだけ owner がインプットすれば良いイメージ。」

**現状は、私（設計者）が 5KB の起動プロンプトを毎回手書きしている。**
作業ディレクトリ・サーバ URL・プロジェクト・作業・識別子・ロール・読む順番・禁止事項を
すべて列挙していた。これは並行本数を増やすほど重くなり、書き間違いも起きる。
**このサービスの目的（スケーラビリティ）と逆行している。**

#### 目標とする体験

```
（実装者エージェントのセッションで、オーナーがスキルを呼ぶ）

  参加できるチャットルーム:
    1. agent-orchestrator / orchestrator-mvp   実装者枠 implementer      在席（心拍 12秒前）
    2. agent-orchestrator / ui-review          実装者枠 ui-implementer   不在（心拍なし）
  どれに入りますか:

（オーナーが「2」と答える。オーナーの入力はこれ1回だけ）

  → 接続設定を書き、handoff / CONTEXT / ADR を pull し、未読メッセージを読み、
    自己駆動ループを開始し、着手表明を投稿する
```

**オーナーが打つのは番号ひとつ。** それ以外はスキルが自分で解決する。

#### そのために必要なもの

エージェントが知らないことは3つある。**ルーム選択だけで残り2つが決まる形にする。**

| 知らないこと | 解決方法 |
|---|---|
| **サーバの場所** | `AO_SERVER_URL` 環境変数 → **リポジトリの `.ao/config.json`** → `~/.ao/config.json`（ユーザ単位の既定値）の順に解決する |
| **どの部屋か** | サーバから一覧を取得して提示し、オーナーが選ぶ。**これが唯一の入力** |
| **何と名乗るか** | **作業が「実装者枠」を持つ。** 部屋を選べば識別子とロールが決まる。
ただし**識別子はリポジトリ設定だけに頼らない**（[ADR 0010](../adr/0010-identity-is-per-agent-not-per-repository.md)）。
`--identifier` / `AO_IDENTIFIER` が上位にある |

#### 実装者枠（expected participant）

**作業は、期待する参加者の識別子とロールを持てる。** 設計者が作業を作るときに宣言する。

- `ao create-work <slug> --title <t> --implementer <identifier>` のように宣言する
- 一覧 API はルームごとに枠と、その枠の在席状況（心拍・ボール・離脱）を返す
- 参加するエージェントは、選んだルームの枠をそのまま名乗る
- 枠が埋まっていて別人が在席中なら、**警告してから確認を取る**
  （同じ識別子を2つのプロセスが名乗ると心拍が混ざり、離脱判定が壊れる）
- 枠が宣言されていない作業は、識別子を尋ねる（従来通り。往復が1回増える）

これは [ADR 0004](../adr/0004-no-authentication.md) の「識別子は自己申告」を覆さない。
サーバは名乗りを検証しない。枠は**入力の手間を減らすための既定値**であって、認証ではない。

#### スキルが自分で行うこと

1. サーバを解決し、ルーム一覧を取得して提示する
2. オーナーの選択を受けて `.ao/config.json` を書く（無ければ `inject` 相当も行う）
3. `handoff` / `CONTEXT` / `ADR` を pull する
4. **未読メッセージを全部読む**。自分宛の未回答 `question` があれば最優先で扱う
5. 自己駆動ループを開始する（§6.2.1）
6. 着手表明を投稿する

**5KB の起動プロンプトに書いていた内容は、すべてスキル側に移す。**
オーナーが毎回貼り付けるものは無くす。

### 6.2.4 設計者は「プロジェクト名だけ」で始められること（オーナー指示・2026-07-26）

オーナーの言葉:
「designer を立ち上げる時も、project 名だけ入れれば良い形式にしたいね」。

§6.2.3 の実装者側と対称にする。**設計者の入力はプロジェクト名ひとつ。**

#### 実装者との違い: 単位がプロジェクトであること

実装者は**ひとつの作業**に入るが、設計者は**プロジェクト全体**を見る。
CONTEXT と ADR はプロジェクトに属し、設計者はその正本の書き手だからである
（[ADR 0002](../adr/0002-passive-server-per-thread-designers.md) の訂正注記を参照）。

したがって設計者のスキルは、**プロジェクト配下のすべての作業を同時に監視する**。

#### 新規プロジェクトも同じ入口で始まる

**指定されたプロジェクトが存在しなければ、作成して設計を始める。**
既存なら参加する。オーナーは「存在するかどうか」を意識しなくてよい。

| 状態 | スキルの動作 |
|---|---|
| プロジェクトが**ある** | CONTEXT / ADR / 全 HANDOFF を pull し、全作業の未読を読み、監視を開始する |
| プロジェクトが**ない** | プロジェクトを作成し、**骨格グリルから始める**（CONTEXT も ADR もまだ無い状態） |

#### スキルが自分で行うこと

1. サーバを解決する（§6.2.3 と同じ3段: `AO_SERVER_URL` → リポジトリの `.ao/config.json` → `~/.ao/config.json`）
2. プロジェクト名を受け取る。**これが唯一の入力**。無ければ一覧を出して選ばせる
3. 存在しなければ作成する
4. CONTEXT / 全 ADR / 全 HANDOFF を pull する
5. **プロジェクト配下の全作業の未読を読む**。自分宛の未回答 `question` を最優先で扱う
6. 全作業を対象に自己駆動ループを開始する（`ao watch --project` 相当）
7. 各作業のボール・離脱・手空きを把握し、必要なら介入する

#### 設計者スキルが保持すべき規律

起動プロンプトに書いていた設計者側の規律を、すべてスキルへ移す。

- **文書先行**。決定はチャットで完結させず、先に CONTEXT / ADR / HANDOFF を更新してから返す
- **判断の三分類**（設計者が決める / オーナーに聞く / 実装者に委ねる）
- **マイルストーン報告は独立検証する。** テスト通過を完了の証拠にしない。
  成果物がディスク上に存在するかを実見する。**心拍を作業の証拠にしない**（差分を見る）
- **旧仕様を残さない。** 決定を変えたら全文を検索して該当箇所をすべて書き換える
- **resolve は監視の終了ではない。** 離脱の許可を出せるのはオーナーだけ
- **オーナー固有の事実を推測しない**（環境・アカウント・ランタイム機能名）

### 6.3 `ao import <path>` — 既存ファイルの取り込み

現行の規約に従ったリポジトリから取り込む。

| 取り込み元 | 取り込み先 |
|---|---|
| `CONTEXT.md` | `kind=context` の文書、revision 1 |
| `docs/adr/NNNN-*.md` | `kind=adr`。**ファイル名の番号をそのまま `adr_number` に使う**（採番し直さない） |
| `docs/handoff/<作業名>.md` | `kind=handoff`。作業を `<作業名>` で作成 |
| `docs/session/<作業名>.jsonl` | 同名の作業のメッセージ |

jsonl の変換規則:

- `id`（`msg-0001`）は**捨てる**。`seq` はファイル内の行順で振り直す
- **元 ID を `message_ref` に `imported-id:msg-0001` として残す。**
  現行運用は同時書き込みで ID が重複することを許容しており（SESSION-PROTOCOL §2.5）、
  `id` を一意キーとして扱うとインポートが落ちる。**行を落としてはならない**
- `reply_to` は元 ID から新 `seq` へ引き直す。**元 ID が重複していて解決できない場合は、
  `reply_to` を NULL にし、`message_ref` に `unresolved-reply-to:msg-0024` を残す**
  （黙って捨てず、後から追える形にする）
- `ts` はそのまま `created_at` に入れる。タイムゾーン付きなら UTC に正規化する
- `from` はそのまま識別子とする。ロールは `from` の文字列から推定し、
  判定できないものは `implementer` にして**インポート結果に一覧で出す**
- 存在しない type は `message` に落とし、同じく一覧で出す

**インポートは冪等でなくてよい**（同じディレクトリを2回入れれば2つ入る）。
ただし**実行前に取り込む件数の内訳を表示し、確認を取ること**。

#### 取り込み前に全件を検証すること（実測で発見・2026-07-26）

**移植元の履歴は壊れている前提で作る。** 実際の移植対象（medops/anonymizer、107メッセージ）を
検証したところ、1件だけ `ts` が `2026-07-26T01:28:58:z` になっていた。
BSD の `date` が `%:z` を解釈できず壊れた値を書いたもので、**このワークフローの運用で
実際に混入する事故である**（本プロジェクトの実装者も同じ事故を起こし自己訂正している）。

現状の実装は次の通りで、**2点は正しく、1点が足りない**。

| 挙動 | 評価 |
|---|---|
| 不正な `ts` を 400 で拒否し、壊れた値を取り込まない | **正しい** |
| 失敗時に部分的な書き込みを残さない（work / document / message とも 0 件へ巻き戻る） | **正しい** |
| **不正行を1件目しか報告せず、そこで止まる** | **不足** |

1件目で止まると、107件の移植で「直す → 再実行 → 次が出る」を繰り返すことになる。
**取り込みを始める前に全行を検証し、不正な行をすべて列挙すること。**
1回の実行で、直すべき箇所が全部分かる状態にする。

さらに `--repair-timestamps` を設けてよい。**曖昧さなく直せるものだけ**を直し
（同一ファイル内の他の行がすべて同じオフセットを持つ場合など）、
元の値を `message_ref` に `imported-ts:<元の値>` として残し、直した件数を報告する。
**タイムゾーンを推測して黙って埋めてはならない。**

---

## 7. 要検証事項

グリルで確定できなかった外部事実。**結果を `status` で報告すること。**
設計と食い違う事実が出たら、実装を進める前に `question` を立てること。

> **訂正（2026-07-26、設計者）**: 本節は当初「実装前の要検証事項」とし、4件すべてを
> 着手前のゲートとして書いていた。これは設計者の誤りである。§7.2 と §7.4 は
> **動くコードが無ければ検証できない**ため、字義通りに守ると実装が始まらない。
> 下記の通り、着手前ゲートと「スパイクで確認するもの」に分類し直した。

| | 内容 | いつ確認するか |
|---|---|---|
| §7.1 | ssh 先の Node バージョン | **着手前**（オーナーへの確認。コード不要） |
| §7.2 | Monitor が `ao watch` の出力を拾えるか | **最小スパイクで**（本実装より前、ただしコードは書く） |
| §7.3 | docker の 127.0.0.1 バインドと port forward の疎通 | **着手前**（既製イメージで確認できる。コード不要） |
| §7.4 | WAL での同時アクセス | **本実装の途中**（サーバが動いてから。設計の見直しが要る場合のみ question） |

**スパイクは使い捨てでよい。** §7.2 は「10秒ごとに1行吐くだけのスクリプト」で足りる。
`ao watch` の本実装を待つ必要はない。§7.4 も同様に、スキーマだけ作って
並行読み書きを叩くだけでよく、API 一式を待たなくてよい。

**§7.1 の回答待ちで手を止めないこと。** Node バージョンが効くのはサーバ側の
SQLite ドライバ選定だけであり、CLI・API 設計・スキーマ・web はどれも先に進められる。

### 7.1 実行環境（クローズ: 2026-07-26 実測）

実測結果:

| | ssh 先 (`ssh i-sugita`) | ローカル (Mac) |
|---|---|---|
| Node | **v22.22.0** | **v22.14.0** |
| npm | 10.9.4 | 10.9.2 |
| OS | Ubuntu 24.04.3 LTS | macOS 26.5.2 |
| arch | **x86_64** | **arm64** |
| shell | bash | zsh |
| Docker | 29.1.5 | （未確認） |

`node:sqlite` は ssh 先の v22.22.0 で**フラグ無しで動作する**
（`DatabaseSync, StatementSync, constants, backup` が取得でき、`ExperimentalWarning` が出る）。

> **訂正（設計者）**: 本項は当初「ホストの Node バージョンが SQLite ドライバの選択に効く」と
> 書いていたが、これは誤りである。**サーバは docker の中で動くため、コンテナの Node は
> 実装者がイメージで選べる。ホストの Node は制約にならない。**
> ホストの Node が縛るのは CLI だけであり、CLI は SQLite を触らない。

**この実測から確定する制約:**

1. **CLI は Node 22.14 以上で動くこと**（ローカル Mac が下限。ssh 先は 22.22）。
   22.14 に無い API を使わない
2. **CLI は arm64（Mac）と x86_64（Linux）の両方で動くこと。**
   ネイティブアドオンを含めると2アーキテクチャ分の配布が必要になるので、
   **CLI は純 JS で書くことを強く推奨する**（依存に native binding を持ち込まない）
3. サーバ側の SQLite ドライバは**実装者の判断に委ねる**。
   `node:sqlite`（Experimental 警告が出る／ビルド不要）でも
   `better-sqlite3`（安定／イメージにビルド環境が必要）でもよい。
   選んだ理由を `status` で報告すること
4. docker イメージは **x86_64 向けでよい**（ssh 先が x86_64）。マルチアーキテクチャ対応は不要

- **調整した節**: §8（配布）に上記1・2・4を反映済み

### 7.2 Claude Code の Monitor ツールが `ao watch` の出力を拾えるか

- **確認すること**: **使い捨てのスパイクで足りる。** 10秒ごとに1行を標準出力へ吐くだけの
  スクリプトを background bash で走らせ、その1行が Monitor 経由でエージェントに届くことを
  **実機で**確認する。`ao watch` の本実装を待たない
- **なぜ**: これが動かないと、サービス化の中心的な利点（通知駆動）が成立しない。
  現行の行数ポーリングに戻すことになる
- **結果次第で調整する節**: §3.1 と §6.2。届かない場合は
  `watch` の出力形式（1行 JSON か人間可読か）を変える余地がある。
  **どちらが拾われやすいか実測して報告すること**

### 7.3 docker の 127.0.0.1 バインドと ssh port forward の疎通

**クローズ（2026-07-26、実装者が実測）。** 経路は成立し、**設計が1つ変わった**（§2 の訂正注記）。

実測:

| | 結果 |
|---|---|
| `i-sugita` 上で `nginx:alpine` を `-p 127.0.0.1:28080:80` で起動 | `docker port` が `80/tcp -> 127.0.0.1:28080`。リモート内 `curl` が exit 0 / HTTP 200 |
| ローカルから `ssh -N -L 127.0.0.1:28081:127.0.0.1:28080 i-sugita` | `lsof` が `TCP 127.0.0.1:28081 (LISTEN)`。ローカル `curl` が exit 0 / HTTP 200 |
| **失敗系**: コンテナ内 bind を `127.0.0.1` に変更（host publish は同じ） | `curl` が exit 52 / `Empty reply from server` |

- **接続先**: `ssh i-sugita`（Ubuntu 24.04.3 / x86_64 / Docker 29.1.5）
- **この検証から出た設計変更**: 失敗系が示す通り、**コンテナ内部 bind を `127.0.0.1` に
  固定すると docker publish が届かない。** 露出の境界は bind ではなく host publish 側にある。
  §2 の表と受け入れ基準18を差し替え済み

### 7.4 WAL モードで web と CLI の同時アクセスが問題ないか

- **確認すること**: **本実装の途中でよい。** §4 のスキーマだけ作り、
  読み取りを複数本走らせながら書き込みを叩いて `SQLITE_BUSY` で落ちないことを確認する。
  API 一式や web の完成を待たない
- **なぜ**: 10秒ポーリングが N 本走る。読み取りが多く書き込みが少ない形なので
  WAL で足りる想定だが、実測していない
- **結果次第で調整する節**: §4。落ちるなら書き込みを直列化するか busy_timeout を設定する

---

## 8. 配布と設定

### サーバ

**デプロイ先は `i-sugita`、動かすのは `main` ブランチ**（オーナー指示・2026-07-26）。
作業ブランチからは動かさない。したがって**マージが完了するまで本番は更新できない**。

**DB の置き場所**（[ADR 0009](../adr/0009-database-on-local-disk-backup-to-s3.md)）:

| | 場所 | 理由 |
|---|---|---|
| **DB 本体** | ホストの**ローカルディスク**上の永続ディレクトリを bind mount | 挿入 3 ms / 読取 0.01 ms |
| **バックアップ** | **`/data`**（S3File 経由の NFS） | 7 MB を `VACUUM INTO` で 266 ms。S3 の耐久性 |

**`/data` に DB 本体を置かない。** 実測で挿入 60.5 ms・読取 11.4 ms（ローカルの20倍・1000倍）。
さらに S3File ゲートウェイの障害がトランザクションを直撃する。
**WAL だけを別ストレージへ逃がすことは SQLite では不可能**（`-wal` / `-shm` は
DB ファイル名から導出され同じディレクトリに置かれる）。

#### 移行中の書き込みを失わないこと（実測で発見・2026-07-26）

現在の `deploy.mjs` は、**移行スナップショットを取ってから `docker build` を実行し、
その後に旧コンテナを削除する**。したがって**ビルド中に書き込まれた分は失われる。**

これは仮定ではない。本番移行の時点で `medopsanonymizer` はライブで、
デプロイ中も書き込みが続いていた。設計者は先にビルドしてから `--skip-build` で
実行し、窓を数秒に縮めて回避した。**手順で回避すべきものではない。**

正しい順序は「書き込みを止める → スナップショット → 新コンテナ起動」だが、
スナップショットは `docker exec` で旧コンテナに入るため、単純に止めると取れない。
**書き込みを止めた状態でスナップショットを取る経路を設計すること**（読み取り専用モードでの
再起動、公開ポートの一時的な取り外し、その他の手段は実装者に委ねる）。

**移行の前後で件数が一致することを、デプロイ自身が検証して出力すること。**
一致しなければ新コンテナを起動せず、非ゼロ終了する。

**コンテナを作り直すと DB が消える構成にしない。** 名前付きボリュームではなく、
ホストのローカルディレクトリを bind mount する（場所は実装者が決めてよいが、
`docker rm` で消えないこと）。

- docker イメージ1つ
- **x86_64 向けでよい**（デプロイ先 `i-sugita` は Ubuntu 24.04.3 / x86_64 / Docker 29.1.5）。
  マルチアーキテクチャビルドは不要
- コンテナ内の Node バージョンとサーバ側 SQLite ドライバは**実装者が選ぶ**。
  ホストの Node に縛られない（§7.1）
- **コンテナ内の bind は `0.0.0.0`。ホスト側の publish は `127.0.0.1:PORT:PORT` に限定する**
  （§2 の表を参照）。compose を同梱するなら `host_ip: 127.0.0.1` を固定で書く
- 起動手順・`docker run` の実例・ボリュームの場所を README に書く。
  **オーナーが手作業でデプロイするので、コピペで動く形にすること**

### CLI

- **Node 22.14 以上で動くこと**（ローカル Mac が v22.14.0 で下限。ssh 先は v22.22.0）
- **arm64（Mac）と x86_64（Linux）の両方で動くこと。**
  ネイティブアドオンを含めると2アーキテクチャ分の配布が要るため、
  **純 JS で書くこと**（依存に native binding を持ち込まない）を強く推奨する
- **追加のランタイム導入なしに ssh 先へ置けること。** 配布方法（単一ファイル / npm / tarball）は実装者に委ねる
- 接続先は `.ao/config.json`。環境変数で上書きできてよい

### 対象リポジトリに置かれるもの

```
<repo>/
├── .ao/
│   ├── config.json        サーバ URL / プロジェクト / 参加者識別子 / ロール
│   ├── state.json         写しの pull 時リビジョン
│   └── docs/              写し（CONTEXT / ADR / HANDOFF）
├── .claude/skills/        サービス版スキル
└── .gitignore             `/.ao/` を追記
```

---

## 9. UI / 出力要件

### web ビューア（オーナーの endpoint）

**必須**:

1. プロジェクト一覧 → プロジェクト詳細（文書一覧・作業一覧）
2. 文書の現在状態の表示（Markdown レンダリング）と、**リビジョン履歴・任意リビジョンの表示**
   （git 履歴を捨てた代替なので、これが無いと変更の追跡手段が消える）
3. スレッドの会話履歴（type・from・宛先・reply_to の関係が読み取れること）
4. **プロジェクト横断の「オーナー宛の未回答 question」一覧**と、そこからの `answer` 投稿
5. **オーナー起点の入力** — 任意の作業へ `message` / `decision` を投稿できる
6. 各作業の**参加者一覧とその状態**（ボール有無・心拍・離脱）

**必須ではない**: 検索、リビジョン間の差分表示、文書編集、リアルタイム更新（リロードでよい）

#### 表示言語（2026-07-26、オーナー指示で追加）

**web ビューアの UI は日本語にする。** 唯一の利用者であるオーナーが日本語話者であり、
文書もスレッドも日本語で書かれているため、UI だけ英語だと視線が行き来する。

対象は画面上の固定文言すべて — ナビゲーション、見出し、テーブルのヘッダ、ボタン、
状態ラベル（ボール有無・心拍・離脱・手空き）、空状態のメッセージ、日時の書式（JST 表示）。
`type`（`question` / `answer` / `status` など）のような**データの値は翻訳しない**
（`CONTEXT.md` の用語と1対1で対応させる必要があるため）。

**CLI の出力は英語のままでよい。** 主な読み手がエージェントであり、
終了コードとエラー文言を README の表と一致させておくほうが追跡しやすい。
これを変えたい場合はオーナーに確認すること。

### CLI の出力

- `ao watch` は新着・手空き通知・離脱警告を**1行ずつ**出す。行の頭で種別が判別できること
- **エラーを黙って握りつぶさない。** 失敗は標準エラーへ理由を出し、非ゼロ終了する。
  `watch` は再試行を続けるが、連続失敗は出力に出す
  （サーバが落ちたまま静かに待ち続ける状態を作らない）

**終了コード（実装済み・実測でクローズ）**。エージェントが `ao push && ao post` のように
連鎖させるため、これは仕様である。値の意味を変えないこと。

| コード | 意味 | 呼び出し側がすべきこと |
|---|---|---|
| `0` | 成功 | 次へ進む |
| `1` | リクエスト不正（HTTP 400）、サーバ到達不能、その他の非衝突エラー | **止まる。** 設計者へ報告するか、入力を直す |
| `2` | 人の判断が要る: リビジョン衝突（409）、未 push 保護による pull 中断、未確認の import | **読み直して編集を当て直し、再試行する** |

> 実装当初、409 が `exit 0` を返す経路があった（top-level await の実行形態に依存していた）。
> `ao push && ao post` が衝突を素通りして次へ進むため、**静かに壊れる**類の欠陥だった。
> 明示的な `process.exit(code)` で曖昧さごと解消済み。

---

## 10. 受け入れ基準

「動く」系:

1. `ao inject <repo>` の実行後、対象リポジトリに `.ao/config.json`・`.ao/docs/` の写し・
   サービス版スキルが存在し、`.gitignore` に `/.ao/` がある
2. 設計者が `ao push` した文書が、実装者側の `ao pull` で取得できる（別マシンでも）
3. `ao watch` が新着を10秒以内に1行として出力する
4. web でプロジェクトの文書・リビジョン履歴・会話履歴が閲覧できる
5. web からオーナーが投稿したメッセージが、CLI 側の `ao watch` に現れる
6. docker で起動したサーバへ、ssh local port forward 経由で CLI が疎通する

ドメイン固有の正しさ（**ここが本質。「動く」だけでは受け入れない**）:

7. **同一文書へ2つのクライアントが同じ `base_revision` で同時に PUT すると、
   片方が 200、もう片方が必ず 409 になる。両方成功することがない**
8. **メッセージを N 本同時に POST しても `seq` が重複せず、欠番も出ない**
9. **同じ `idempotency_key` で2回 POST しても、メッセージは1件しか作られない**
10. **宛先が2人の question に1人が answer しても、もう1人のボールは消えない。
    質問者が `close` すると両方消える**
11. **導出ボール（未回答 question）は、宣言ボールでは消せない**
12. **ボールを持つ参加者の `poll` が3分途絶えると、他の参加者の `poll` 応答の
    `abandoned` にその参加者が現れる**
13. **`resolve` した後も、離脱判定と手空き通知が動き続ける。
    resolve を根拠に離脱した参加者は `abandoned` に載る**
14. **ボールが無い参加者が5分沈黙すると `idle_nudge` が返る。
    その参加者が何か書くと、次のポーリングでは `idle_nudge` が消える**
15. **`ao pull` は、未 push の写しの編集を `--force` 無しに破棄しない**
16. **写しを編集しただけでは正本のリビジョンが上がらない**
17. **重複した `id` を含む jsonl をインポートしても行が1つも失われず、
    元 ID が `message_ref` に残る。解決できない `reply_to` は NULL になり、
    その旨が `message_ref` に残る**
18. **露出の境界が配布形態ごとに正しい**（当初の「`0.0.0.0` 経路を一切含めない」は
    docker 配布と両立しないため差し替えた。§2 の訂正注記を参照）。内訳:
    - a. **直接起動で引数・環境変数を何も与えないとき、bind は `127.0.0.1`**。`0.0.0.0` にならない
    - b. **docker の起動手順・スクリプト・compose のすべてで host_ip が `127.0.0.1` に固定されている。**
      `-p PORT:PORT`（host_ip 省略）の例がリポジトリ内のどこにも存在しない
    - c. 起動ログに `listening on <addr>:<port>` が出る。`0.0.0.0` のときは警告が添う
    - d. docker で起動したサーバへ、**ホストの外部インタフェース**（`ホストのLAN IP:PORT`）から
      到達**できない**ことを実測で確認する。ループバックからは到達できる
19. **サービス版の手順に、廃止された記述（`jq -nc` での採番、`wc -l` ポーリング、
    `msg-NNNN` の手採番、ID 衝突時の回避手順）が1つも残っていない**
20. **注入が §6.1 の4箇所すべてへ配る。`CLAUDE.md` と `AGENTS.md` は追記のみで
    既存内容を壊さず、同じリポジトリへ2回注入しても記述が重複しない**
21. **`.claude/skills/` と `.agents/skills/` の内容が完全に一致する**
    （単一ソースから生成されていることの確認。片方だけ更新される事故を防ぐ）
22. **常駐 `ao watch` のループが心拍を更新しない。**
    常駐 watch だけを走らせた状態で放置すると、ボールを持つ参加者が
    離脱として検知される（プロセスの生存が注意の生存を偽装しない）
23. **`ao watch --once` および `post` / `pull` / `push` / `messages` は心拍を更新する**
24. **サービス版スキルの監視の節が §6.2.1 の5項目を満たす。**
    とくに「自己駆動ループの具体例」と「心拍が途絶えると離脱扱いになる旨の告知」が
    書かれていること。**「`ao watch` を起動して待て」で終わる記述が残っていないこと**
25. **§6.2.2 の同梱スクリプト1〜4がスキルの一部として配られ、`inject` で対象リポジトリに置かれる。**
    **注入したリポジトリの中で、追加の設定なしに実際に動くこと。**

    > **追記（2026-07-26、実測で不合格）**: 当初「実際に実行して動くこと（散文の例ではなく
    > 実行可能なファイルであること）」とだけ書いていたため、
    > **ファイルとしては存在するが実行すると落ちる**状態が基準を通ってしまった。
    >
    > 実測: 注入先で4本すべてが `cannot start ao: spawn ao ENOENT` で失敗した。
    > スクリプトは `process.env.AO_CLI ?? "ao"` で CLI を解決するが、
    > 注入先には `ao` が PATH にも `node_modules/.bin/` にも無く、
    > `.ao/config.json` にも場所が記録されていない。`AO_CLI` はスキル本文にも出てこない。
    >
    > **`inject` が CLI の起動方法を `.ao/config.json` に記録すること。**
    > スクリプトは次の順に解決する。
    >
    > 1. **`AO_CLI` 環境変数**（明示的な上書き。最優先）
    > 2. `.ao/config.json` の記録（`inject` が書く。通常はこれが使われる）
    > 3. PATH 上の `ao`（どちらも無い場合の最後の手段）
    >
    > いずれも解決できなければ、**何を探して見つからなかったかを示して非ゼロ終了する。**
    > `ENOENT` をそのまま出さない。
    >
    > スキル本文にも `AO_CLI` による上書き手段を書く。
    >
    > 訂正: 当初この順を「config の記録 → `AO_CLI` → PATH」と書いていたが、
    > **注入後は config の記録が必ず存在するため、その順では `AO_CLI` が一度も選ばれない。**
    > 同じ段落で `AO_CLI` を「上書き手段」と呼んでいたのと矛盾していた（実装者の指摘）。
    >
    > 検証は**注入したリポジトリの中で**行うこと。開発リポジトリのルートで動いても意味がない
    > （そこには `bin/ao.js` があるので、注入先の状況を再現しない）。
26. **投稿ラッパが、`reply_to` の無い `answer` と `to` の無い `question` を
    送信前に落とし、非ゼロ終了する。** サーバの 400 を待たない
27. **監視スクリプトが日本語本文を含むスレッドで誤判定しない。**
    取得失敗と「新着ゼロ」を区別し、連続失敗を出力に出す
28. **`scripts/` の DB バックアップが、WAL モードで実データを含むバックアップを作る。**
    取得後に各テーブルの件数を表示し、空でないことを確認できる
    （`cp ao.sqlite` では中身が入らないことを実測済み）

29. **実装者エージェントは、スキルを呼ぶだけで参加できる**（§6.2.3）。
    **オーナーの入力はルーム選択の1回だけ**で、起動プロンプトの貼り付けは不要
30. **ルーム一覧が、作業ごとの実装者枠と在席状況（心拍・ボール・離脱）を出す**
31. **作業に実装者枠を宣言でき、参加時にそれが既定の識別子になる。**
    枠が埋まっていて別人が在席中なら警告して確認を取る
    （同じ識別子を2プロセスが名乗ると心拍が混ざり、離脱判定が壊れる）
32. **サーバの場所が `AO_SERVER_URL` → リポジトリの `.ao/config.json` → `~/.ao/config.json`
    の順に解決される。** 注入前のリポジトリでもスキルが動く

    > **訂正（2026-07-26、実測で発覚）**: 当初この順を
    > 「`AO_SERVER_URL` → `~/.ao/config.json` → リポジトリの `.ao/config.json`」と書いていた。
    > **これは設計者の誤りである。ユーザ単位の既定値がリポジトリ固有の設定を上書きしてしまう。**
    >
    > 実害は2つ。第一に、**スキルが指示する `ao configure` を実行した瞬間にテストが壊れる**
    > （実測: `~/.ao/config.json` を置くと 45/45 が 35/45 へ。退避すると 45/45 に戻る）。
    > 第二に、本番では**注入済みリポジトリがサーバ A を指していても、
    > ユーザ既定のサーバ B へ黙って送られる**。データが誤ったサーバへ行く。
    >
    > `~/.ao/config.json` は「リポジトリ設定が無いときの既定値」であり、
    > **上書きするものではない。** 明示的な上書きは `AO_SERVER_URL` だけが担う。
40. **ユーザ設定 `~/.ao/config.json` が存在しても、リポジトリの `.ao/config.json` が優先される。**
    テストスイートが `~/.ao/config.json` の有無に影響されないこと
41. **プロジェクト・作業・参加者を削除できる。** `confirm` に対象 slug を渡さなければ 400。
    削除はカスケードし、トランザクションで行い、中途半端な状態を残さない
42. **削除の応答が、何を何件消したかの内訳を返す**
43. **発言が1件でもある参加者の削除は 409 で拒否される。** 未発言の誤登録だけが消せる
44. **メッセージ単体を削除する手段が存在しない**（追記のみの不変条件を維持）
45. **DB 本体がローカルディスク上にあり、`docker rm` で消えない**（bind mount）
46. **`/data` へ定期バックアップが取られ、世代が残る。**
    間隔と世代数は実装者が決めてよいが、README に明記すること
47. **復元手順が文書化され、実際に復元して件数が一致することを実測で確認してある。**
    取れているつもりのバックアップは、復元するまで取れていない
48. **ローカルディスクの空きが枯渇したときに気づける**（バックアップ時に空きを出す等）
49. **メッセージが1件でもある対象の削除は、既定で 409 で拒否される。**
    応答に、作業ごとのメッセージ数・最終更新時刻・心拍のある参加者が含まれる
50. **空でない対象を消すには、`confirm` とは別の明示的な指定が要る。**
    `confirm` の使い回しでは通らない
51. **CLI は削除前に、失われる内容の要約を必ず表示する**（空の対象でも表示する）
52. **デプロイの移行が、書き込みを止めた状態でスナップショットを取る。**
    ビルド中の書き込みが失われない
53. **デプロイが移行前後の件数一致を自分で検証し、一致しなければ新コンテナを起動せず
    非ゼロ終了する**
54. **識別子とロールが「`--identifier`/`--role` → `AO_IDENTIFIER`/`AO_ROLE` →
    リポジトリ設定」の順に解決される**（[ADR 0010](../adr/0010-identity-is-per-agent-not-per-repository.md)）。
    サーバ URL とは順序が逆であり、それは意図的である
55. **`post` / `messages` / `watch` / `pull` / `push` / `close` / `resolve` のすべてが
    `--identifier` を受け付ける。** `join` だけではない
56. **同一リポジトリを2体のエージェントが共有し、それぞれ `AO_IDENTIFIER` を与えた状態で、
    投稿の `from` が取り違えられないことを実測で確認してある**
57. **`--to designer` の question に designer が answer したとき、
    共有リポジトリでも未回答が解消され、`abandoned` が消えることを実測で確認してある**
    （回答者の名義が入れ替わると、回答済みでも未回答のまま残る事故が実際に起きた）
58. **`join` / `inject` / `design` が、既に別の識別子で使われているリポジトリ設定を
    上書きするときに警告する**

    > **訂正（2026-07-26、実装者の指摘による）**: 当初ここを「`join` / `configure`」と
    > 書いていたが誤りである。**`configure` は識別子を扱わない。**
    > 実測すると `ao configure --server URL --identifier evil` は
    > `{"server_url": "..."}` だけを書き、`--identifier` を黙って捨てる。
    > リポジトリ設定の識別子を書くのは `join` / `inject` / `design` である。
59. **既存の作業に実装者枠を後から宣言できる。** 現状 `create-work --implementer` しか無く、
    既存の作業は `slot=undeclared` のままで、§6.2.3 の「番号ひとつで参加」が成立しない
    （実測: PATCH / PUT ともルートが存在せず 404）
33. **参加後、スキルが自分で pull・未読読み込み・自己駆動ループ開始・着手表明まで行う**
34. **設計者エージェントは、プロジェクト名ひとつで始められる**（§6.2.4）。
    起動プロンプトの貼り付けは不要
35. **指定したプロジェクトが存在しなければ作成し、骨格グリルから始まる。**
    存在すれば参加する。オーナーは存在の有無を意識しなくてよい
36. **設計者はプロジェクト配下の全作業を同時に監視する。**
    どの作業でボール・離脱・手空きが起きているかを1つのセッションで把握できる
37. **設計者スキルが §6.2.4 の規律6項目を保持している。**
    とくに「心拍を作業の証拠にしない（差分を見る）」と
    「旧仕様を残さない（決定を変えたら全文検索する）」が書かれていること。
    どちらも設計者が実際に踏んだ失敗である
60. **識別子とロールを `~/.ao/config.json`（ユーザ設定）へ保存する経路を作らない。**
    ユーザ単位の既定識別子は、1人が複数のエージェントを動かす前提と矛盾する
    （すべてのエージェントが同じ名前を名乗る）。
    既存のユーザ設定に識別子が残っている場合は**警告して無視する**。採用してはならない
61. **適用されないオプションを黙って捨てない。** 受け付けないオプションが渡されたら
    非ゼロで失敗する（例: `configure --identifier`）。
    黙って無視されると、エージェントは「識別子を設定した」と誤認する。
    これは本節が直している事故と同じ形である
62. **`session-chat` の起動手順が番号つきの必須ステップであり、
    (a) 専用 worktree の作成と (b) 定期自己確認の登録を、手順の中に含む。**
    説明文の中の注意書きではなく、実行すべきステップとして書かれていること
63. **起動報告が、worktree のパス・ブランチ・識別子・スケジューラ登録の成否を名指しする。**
    省略したステップが報告に穴として現れること
64. **スケジューラを登録できなかった場合、その事実と代替手段を起動報告に書くことが
    義務づけられている。** 「見つからなければオーナーに聞け」で終わらせない
65. **`design-handoff` にも定期自己確認の登録ステップと起動報告がある。**
    設計者は worktree を作らないことも明記されている
66. **CLI の参照先がリポジトリの外にある。** `.ao/config.json` の `cli.args` が
    リポジトリ内のパスを指してはならない
    （チェックアウトが変わると中身が変わる）
68. **CLI のインストール手順がスクリプトとして存在し、完了時にスモークテストで
    動作を確認する。** 手でディレクトリを選んでコピーする手順にしない
69. **文書を削除できる。** `confirm` を要求し、リビジョンもカスケードで消す。
    削除の応答に件数の内訳を返す

    > **実際に起きたこと（2026-07-26）**: 設計者が `create-document adr` で
    > ADR を追加したところ、**サーバが番号を採番して `0009-adr-0011` になった。**
    > git 側では ADR 0011 なので番号が食い違い、しかも**番号9を占有したため
    > 正しい ADR 0009 を取り込めなくなった。**
    >
    > 削除する手段が無いので、この誤りは修復できない。
    > 基準41〜44 で project / work / participant の削除は作ったが、
    > **文書を入れ忘れていた。**
70. **`create-document adr` が、既存の番号と衝突しない番号を採るか、
    番号を明示できる。** ファイル名に番号を持つ ADR を git から個別に登録するとき、
    サーバ採番だと git 側の番号とずれる。`import` は番号を保存するので、
    単発登録にも同じ経路が要る
71. **離脱の判定がプロジェクト全体の活動で行われる。**
    同一プロジェクトの別の作業で心拍が新しい参加者は、
    ある作業の心拍が古くても `abandoned` に現れない
    （[ADR 0006](../adr/0006-ball-tracking-and-heartbeat.md) の追記）。
    **実測で確認すること**: 作業 A のボールを持つ参加者が、作業 B で能動コマンドを
    叩き続けている状態で、3分経っても A で離脱にならないこと
72. **ボールの判定は作業ごとのまま。** 71 の変更でボールの意味を変えない
73. **スキルが「止められる前提」で書かれている。**
    (a) 1ターンで安全にできる限り進める、(b) **ターンが終わる前に再開点つきの
    `status` を投稿する**（done / in-progress / next / blocked-by）が
    手順として書かれていること
### schema バージョンの割り当て（2026-07-26・並行作業のため設計者が調停）

**現在の `main` は v2。** 2つの作業が同時に次のバージョンを使おうとしたため、
設計者が割り当てとマージ順を決めた。

| 作業 | バージョン | 内容 | マージ順 |
|---|---|---|---|
| `issue-tracker` | **v3** | `issue` / `issue_comment` テーブル | **先** |
| `orchestrator-mvp`（基準74） | **v4** | `participant.attendance_mode` | 後 |

**この順序を守ること。** migration が整数バージョンの単調増加を前提にしているため、
後から小さいバージョンを足すと適用されない。

割り当てを変えたい場合は、実装を進める前に `question` を立てること。
**両者が同じ番号を使うと、マージした側の migration が黙って適用されずスキーマが壊れる。**

74. **参加者が出席形態（自走 / 起動待ち）を持ち、
    起動待ちの参加者は `abandoned` ではなく `awaiting_activation` として報告される。**
    起動報告の `schedule=registered|unavailable` を申告として使ってよい
75. **`poll` 応答と web の両方で、離脱と起動待ちが別の語で示される。**
    混ぜてはならない（本当にプロセスが消えたときの通知が埋もれる）
76. **オーナー向けに「いま起こすべき参加者」の一覧が取れる。**
    プロジェクト横断で、起動待ちかつボールを持つ参加者を返す
77. **情報提供だけの `status` でボールを他者へ宣言しない、という規約が
    スキルに書かれている。** 相手に何かを求めるときだけ宣言する
    （求めるものが応答なら `question` を使う）

    a. **宣言したボールを手放す方法がスキルに書かれている。**
       最新の宣言だけが有効なので、`--ball ''` を打てば
       「誰も持たない」という宣言になり解除される

    b. **`--ball ''` による解除が CLI から到達できることが回帰テストで固定されている。**
       宣言 → `has_ball=true`、`--ball ''` → `has_ball=false`、`exit=0`

    > **背景（2026-07-26、設計者の規定不足）**: 77 を追加した時点では
    > 「宣言するな」しか書いておらず、**宣言してしまった後の戻し方を
    > 書いていなかった。** 規約が半分しかない状態だった。
    >
    > 設計者が `issue-tracker` で実装者から宣言されたボールを手放そうとして
    > 気づいた。`Object.hasOwn(input, "ball")` で宣言の有無を判定しているため
    > `ball: []` は成立し、**機構は最初から解除できた。文書だけが欠けていた。**
    >
    > 手放せると知らないエージェントはボールを溜め、持ったまま心拍が古くなり、
    > 偽の離脱通知が出続ける。このプロジェクトで繰り返し起きた失敗である。
    > **宣言を減らすことと、溜まったものを返せることの両方が必要である。**
    >
    > b を要求するのは、実測では通っているが回帰テストで固定されていないため。
    > `optionList` の空文字扱いが変われば、気づかずに壊れる。
78. **スキルに「自分の作業ブランチへの push は事前承認済み」と明記されている。**
    聞かずに待たずに push する。ツールの承認ゲートが確認を求めた場合、
    この規約が承認の根拠になる
79. **スキルに「公開はオーナーの明示的な指示が要る」と明記され、対象が列挙されている。**
    PR の作成とマージ、`main` や他者が保持するブランチへの push、
    デプロイと本番の再起動、空でないものの削除

    > **背景（2026-07-26、オーナー指示）**: `session-chat` が
    > 「Commit and push only when authorized」と書いていたため、
    > 実装者が実装完了後に push を保留した。
    > `issue-tracker` の HANDOFF には「コミットと push は自由」と書いてあったが、
    > **スキルの方が厳しく、実装者はスキルに従った。**
    > 文書間で規約が食い違っていたのは設計者の誤りである。
    >
    > push と公開は別物である。push は取り消しが容易で他者に影響しない。
    > 公開（PR・マージ・デプロイ）は影響が外へ出る。
    > **この2つを「authorized」という同じ語でまとめていたのが原因。**

    > **背景（2026-07-26、オーナー報告）**: Codex app の調子が悪く、
    > カスタム スケジュールが発火しない状態が起きた。
    > **エージェントが進んでいたのは、オーナーが手で叩いていたからである。**
    >
    > ランタイムが不安定なとき、エージェントは止まる。それは避けられない。
    > 避けられるのは**再開のコスト**である。
    > 再開点が残っていれば1メッセージで済み、無ければ状態の再構成から始まる。
    > **止まらない方法が無いなら、止まったことが安く回復できる形にする。**

    > **実際に起きたこと（2026-07-26）**: 設計者が
    > `git archive origin/main bin src package.json` でリポジトリ外へ CLI を取り出したが、
    > **`templates/` を入れ忘れた。** `ao join` はスキルを配置するために
    > `templates/skills/` を読むため、`ENOENT scandir .../templates/skills` で
    > 途中失敗した。`.ao/config.json` だけが書かれ、スキル配置・文書 pull・
    > スレッド表示は行われない中途半端な状態になった。
    >
    > **設計者はこの CLI 配置を2回続けて誤った。** 1回目はリポジトリ内の worktree を
    > 「安定パス」と呼び、2回目は必要なファイルを取りこぼした。
    > 手順を人が組み立てる限り、また間違える。
    >
    > インストールスクリプトは、配置後に**実際に `rooms` と `join` を素の
    > ディレクトリで実行して成功を確認する**こと。ファイルが置かれたことは
    > 動くことを意味しない。
67. **実装者の worktree が `worktree/<作業名>` であり、ブランチ上にある。**
    既存の worktree を流用しない。detached HEAD で作業しない。
    **ブランチが他のチェックアウトで使われている場合は、他方を切り替えず
    `work/<作業名>-impl` を作る**（相手の作業を妨げないため）

80. **文書の題名が本文の見出しに追随する。** 本文の1行目が `# ` 見出しなら、
    一覧・サイドバー・ページ見出し・`<title>`・API の `title` はその見出しを示す。
    見出しが無い文書だけ、作成時の題名にそのまま従う。

    a. **既存の行にも遡って効くこと。** 移行を伴う実装（保存済み `title` の
       書き換え）でも、読み取り時の導出でも、どちらでもよい。判断は実装者に委ねる
    b. **回帰テストで固定すること。** 作成 → 見出しを変えた本文で PUT →
       一覧と文書ページの両方が新しい見出しを示すことを確認する

    > **実際に起きたこと（2026-07-26、設計者が本番で発見）**:
    > `updateDocument` はリビジョン行を挿入して `current_revision` を上げるだけで、
    > **`document.title` に一度も触らない。** 題名は作成時のまま永久に凍結される。
    >
    > 本番の ADR 0006 で実害が出ていた。本文の見出しは訂正後の
    > 「心拍はエージェントの能動的な操作で打つ」だが、保存された題名は
    > **撤回済みの「心拍はポーリングの副産物とする」**のままだった。
    >
    > web の文書ページは保存題名をページ見出しに出し、その直下に本文の見出しを出す。
    > **同じページが自分と矛盾する。** 撤回済みの文言が3回、現行の文言が1回現れた。
    > 一覧・サイドバー・`<title>` もすべて撤回済みの題名を示していた。
    >
    > これは体裁の問題ではない。ADR の一覧は「何が決まっているか」を読む場所であり、
    > **そこが撤回された決定を宣伝していると、読んだ者が撤回済みのルールを実装しうる。**
    > 実際 ADR 0006 は、常駐 `ao watch` の心拍が離脱検知を無効化するという
    > 最も高くついた失敗の訂正そのものである。
    >
    > [ADR 0005](../adr/0005-full-text-put-with-optimistic-lock.md) で本文を全文 PUT の
    > 正本と定めた以上、題名を別に保存すれば第2の情報源になり必ずずれる。
    > 本文から導けるものを二重に持たないこと。

### ブランチの分担（2026-07-26 に定めた。既存の履歴には遡らない）

**設計者は作業ブランチを保持しない。** 実装ブランチは実装者のものである。

この規約は今回の作業では守れていない。設計者がメインチェックアウトで
`work/orchestrator-mvp` を保持し、そこへ設計文書のコミットを重ねたため、
**設計コミットと実装コミットが同一ブランチに交互に乗っている。**
「設計と実装のコミットを分ける」という当初の意図が崩れた状態である。

いま解きほぐす価値は無いと判断した（履歴の書き換えが必要で、
得られるのは見やすさだけ）。**次の作業から適用する。**

実装者は `work/<作業名>-impl` を使い、設計者が後でマージする。

    > **実際に起きたこと（2026-07-26）**: 設計者が「CLI の安定置き場」として
    > リポジトリ内に `worktree/cli` を作り、別リポジトリの `cli.args` にその絶対パスを
    > 記録した。実装者はその worktree を「main checkout ではないから隔離されている」と
    > 判断してそこで作業した。結果、**別リポジトリで `ao` を叩くと実装者の作業中コード
    > （未コミット差分込み）が動く**状態になった。
    >
    > さらに実装者は detached HEAD 上でコミットしており、HEAD が動けば失われる状態だった。
    >
    > 原因は設計者が「リポジトリ内の worktree」を安定パスと呼んだこと。
    > リポジトリ内にある限り、誰かが触る。CLI は
    > `~/.local/share/agents-chat-room-cli/` のようにリポジトリ外へ置く。
    > 依存ゼロなので `git archive` で取り出したコピーがそのまま動く。

    > **背景（2026-07-26、オーナー報告）**: session-chat スキルを Codex の実装者が
    > 実行したところ、**worktree を作らず、カスタム スケジュールも設定せず、
    > まったく機能しなかった。**
    >
    > 原因は構造だった。旧版は180行のうち大半が説明文で、
    > **worktree を作る指示が1つも無く**（`worktree` の語は
    > 「無関係な変更を保持せよ」の文脈で1回だけ）、
    > カスタム スケジュールは100行の散文の後の箇条書きに
    > 「見つからなければオーナーに聞け」という逃げ道つきで置かれていた。
    > **情報としては書かれていたが、実行すべき手順として書かれていなかった。**
    >
    > 設計者が5KBの起動プロンプトを手書きしていた頃、worktree 作成はそこに書かれていた。
    > スキルへ移す際に落とした。

38. **`ao import` が、取り込みを始める前に全行を検証し、不正な行をすべて列挙する。**
    1件目で止まらない。1回の実行で直すべき箇所が全部分かる
39. **不正なデータがあったとき、部分的な書き込みが残らない**（実測で確認済み・維持すること）

---

## 11. リポジトリ現状

- 着手時点で、このリポジトリには `.claude/skills/`（現行スキル4本）、`CONTEXT.md`、
  `docs/adr/0001`〜`0008`、本文書のみが存在する。**実装コードは1行も無い**
- **git / GitHub は準備済み**（2026-07-26、オーナーの指示により設計者が実施）。
  リモートは `github.com:dermasugita/agents-chat-room`、既定ブランチは `main`。
  `main` には README と設計文書のコミットのみが載っている

  > 注記（撤回済みの前提）: 本節は当初「git リポジトリではないので git 操作を行うな」と
  > 規定していた。オーナーから手順が渡されたため撤回し、下記に差し替えた。

- **リポジトリ名は `agents-chat-room`**（ローカルのディレクトリ名 `AgentOrchestrator` とは異なる）。
  §1 の注記の通り、サーバはエージェントをオーケストレーションしない。
  GitHub 側の名前のほうが実態に近い

- **ブランチとコミットの分担**:
  - 実装は **`work/orchestrator-mvp` ブランチ**で行う。`main` へ直接コミットしない
  - **実装コードのコミットと push は実装者が行ってよい。** 進捗報告・レビュー依頼のたびに push し、
    スレッドへ `status` を書いてコミットハッシュを `refs` に添える。設計者はそれを取得してレビューする
  - **設計文書（`CONTEXT.md` / `docs/adr/` / `docs/handoff/`）のコミットは設計者が行う。**
    実装者はこれらを編集しない（指摘は `question` で上げる）
  - **自分の作業ブランチへの push は事前承認済み。** 聞かずに、待たずに push する
    （未 push のコミットを実際に失いかけた。作業ブランチの push は他者に影響しない）
  - **公開はオーナーの明示的な指示が要る。** PR の作成とマージ、`main` や他者が
    保持するブランチへの push、デプロイと本番サービスの再起動、
    空でないものの削除は、指示なしに行わない

- シークレットは無い。認証を持たない設計なので、トークン類は発生しない。
  発生した場合は `.env` に置き `.gitignore` する
