# CONTEXT — AgentOrchestrator 用語集

この文書は**言葉の定義だけ**を置く。実装・仕様・未解決論点は書かない（仕様は `docs/handoff/`、決定の理由は `docs/adr/`）。

> 注意: このリポジトリは「design-handoff ワークフローをサービス化する」プロダクトを作る。
> したがって「プロジェクト」「文書」といった語は、**サービスが管理する対象**を指すのか、
> **このリポジトリ自身**を指すのかで意味が変わる。以下の定義はすべて**サービスが管理する対象**の側である。

---

## 中核概念

### プロジェクト (Project)
サービスが管理する最上位の単位。1つのプロダクト／リポジトリに1つ対応する。
**用語集（CONTEXT）はプロジェクト単位で1つだけ持つ**。プロジェクトの下に複数の作業がぶら下がる。

### 注入 (Injection)
任意の既存リポジトリを、このサービスの管理下に置く操作。
対象リポジトリへ接続設定・**サービス版のエージェント向けスキル**・文書の写しを配置し、
サーバ側にプロジェクトを登録する。

注入がスキルの配布経路を兼ねるのが要点である。サーバと CLI だけ用意しても、
エージェントの行動を決めているのはスキル文書なので、旧来のファイル手順のまま使われてしまう。

### 作業 (Work)
1人の実装者に受け渡される仕事の単位。1つの作業は 1つの HANDOFF と 1つのスレッドを持つ。
複数の作業を同一プロジェクト内で並行させてよい。**CONTEXT と ADR は作業ごとに分けない**（用語集を作業単位で割ると、同じ言葉が別定義を持ったまま誰も気づかない）。

### スレッド (Thread)
1つの作業に対応する会話の場。参加者がメッセージを追記していく。
従来のファイル運用における `docs/session/<作業名>.jsonl` に相当する。

**スレッドの参加者は固定の2者ではなく、人数も顔ぶれも実行中に変わる。**
1人の設計者と3人の実装者、レビュー役の追加、オーナーの途中参加、といった形を取りうる。
設計上「相手」という単数の概念を置いてはならない。

### メッセージ (Message)
スレッドへの1回の書き込み。追記のみで、**編集・削除はできない**。訂正は新しいメッセージで行う。

---

## 文書

### 文書 (Document)
サービスが正本を保持する設計文書。種別は次の3つ。

| 種別 | 個数 | 役割 | 書かないもの |
|---|---|---|---|
| **CONTEXT** | プロジェクトに1つ | 言葉（用語集） | 実装・仕様・未解決論点 |
| **ADR** | プロジェクトに複数 | なぜ（決定と却下案） | 手順・スキーマ |
| **HANDOFF** | 作業に1つ | 何を・どう作るか（仕様） | 決定の背景議論 |

### 正本 (Source of Truth)
その成果物について、**変更を受け付ける唯一の場所**。サービスは**設計文書とスレッドの正本だけ**を持つ。

**コードの正本は GitHub にあり、サービスは関与しない。** 実装者は進捗報告・レビュー依頼のたびに
GitHub へ push し、設計者はそれを取得してレビューする。したがって設計者と実装者は
**チェックアウトを共有する必要がない**（コードは GitHub 経由、文書と会話はサービス経由で届く）。

### 写し (Materialized Copy)
正本からリポジトリの作業ディレクトリへ書き出されたファイル。
参加者が読む・grep する場であり、**文書を書き換えるときの編集面**でもある。

写しへの編集は、**明示的に push するまで正本に反映されない**。自動同期は行わない。
pull すると写しは正本で上書きされ、push していないローカルの編集は失われる。

### リビジョン (Revision)
文書の1回の更新で生まれる版。文書ごとに単調増加する。
「いつ・誰が・何を変えたか」を追える単位であり、従来 git 履歴が担っていた役割を引き受ける。

### 期待バージョン (Expected Version)
参加者が**メッセージを書くとき**に添える、「自分はこの文書のこのリビジョンを読んで発言している」という宣言。
受け手は自分が読んでいるリビジョンと突き合わせ、食い違っていればそのメッセージを処理する前に同期する。
**古い版を前提にした議論が気づかれずに進むことを防ぐための仕組み**であり、ロックではない。書き込みを拒否しない。

### 基底リビジョン (Base Revision)
参加者が**文書を更新するとき**に添える、「この版を土台に書き換えた」という申告。
サーバ側の現在リビジョンと一致しない場合、更新は**拒否される**（楽観ロック）。
拒否された側は最新を読み直してから書き換えを作り直す。

期待バージョンと基底リビジョンは**別の仕組みである**。前者は議論のズレに気づくため（拒否しない）、
後者は他人の更新を気づかず踏み潰すのを防ぐため（拒否する）。片方でもう片方を代用しない。

---

## 参加者

### 参加者 (Participant)
スレッドに書き込める主体。**識別子**（`designer-a`, `impl-web` など、スレッド内で一意）と
**ロール**を持つ。識別子とロールは別物であり、同じロールの参加者が同一スレッドに複数いてよい。

参加者は**事前登録されない**。最初の書き込みで生まれ、いつでも増える。
スレッドの参加者集合は実行中に変化する。

| ロール | 実体 | 責務 |
|---|---|---|
| **オーナー (owner)** | 人間 | 金・アカウント操作・要件変更・対外的な命名を決める。離脱の許可を出せる唯一の存在 |

| **設計者 (designer)** | エージェント | 文書の正本を書き換える。アルゴリズム・整合性・トレードオフを決める |
| **実装者 (implementer)** | エージェント | コードを書く。実装言語・ライブラリ・内部構造を決める |

### ボール (Ball)
「次に動くべきなのは誰か」を指す語。ある参加者にボールがあるとは、その参加者が
**未回答の question を持つ / 引き受けた作業を完了報告していない / やると宣言したことをまだやっていない /
受領した指摘に対応も反論も返していない** のいずれかに当てはまる状態をいう。

いずれにも当てはまらず、かつ自分が投げた question の回答待ちか完了報告への承認待ちであるときのみ、
その参加者にボールは無い。**ボールが自分にある状態で待ちに入ること・会話を終えることは禁じられる。**

**ボールは1人に固定されない。同時に複数の参加者が持ちうる集合である。**
「相手にボールがある」という言い方はしない（スレッドは N 対 N でありうる）。
判定は常に参加者ごとに行う。

サービスはボールを2通りで把握する。**未回答の question**（宛先のうち、まだ answer を返していない
参加者がいる状態）はサーバが自動で導出する。それ以外の3条件は意図の理解が要るため機械判定できず、
参加者が書き込み時に**宣言**する。導出されたボールは宣言では消せない（question に答えるまで残る）。

### 心拍 (Heartbeat)
参加者が生きていることの記録。新着ポーリングのリクエストそのものが心拍を兼ねるため、
参加者は心拍のために特別なことをしない。サーバは参加者ごとの最終ポーリング時刻を持つ。

### 離脱 (Abandonment)
**ボールを持ったまま心拍が途絶えた**状態。作業中の沈黙（心拍はあるが発言が無い）とは区別される。
沈黙は正常だが、離脱は異常である。

### 手空き (Idle)
ボールを持たず、かつ自分も他の参加者も一定時間なにも書き込んでいない状態。
**手空きは「待っていてよい」を意味しない。** 全参加者が互いに「他の誰かのボールだ」と
思い込んだまま固まるデッドロックは、この状態として現れる。

### 手空き通知 (Idle Nudge)
手空きが一定時間続いた参加者に対し、新着ポーリングの出力へ混ぜて送られる催促。
スレッドには書き込まれず、その参加者にだけ届く。受け取った参加者は、
黙って待ち続けるのではなく、状況を問い合わせるか自分の残作業を再確認する。


### オーナー起点の入力 (Owner-Originated Input)
オーナーが web ビューアから直接スレッドへ投じる書き込み。未回答 question への回答だけでなく、
要件変更・指示・承認を能動的に発信することを含む。

**オーナー起点の入力は一次情報である。** エージェント経由で伝えられた「オーナーがこう言った」
（伝聞）とは区別され、本人確認を経ずにそのまま設計文書へ反映してよい。

### resolve
「設計と実装の合意が取れた」という宣言。**セッションの終了ではない。**
resolve 後もコードレビュー・コミット指示・CI 失敗とその修正が控えており、監視は継続される。
